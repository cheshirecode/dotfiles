#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export WORKLOG_MANAGER_USAGE="${WORKLOG_MANAGER_USAGE:-$0}"
exec node "$SCRIPT_DIR/../lib/worklog-manager/cli.mjs" "$@"
