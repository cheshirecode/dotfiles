.DS_Store
*.log
.env
.env.*
.vscode/
.idea/
# Derivative index (rebuilt on demand by bin/index.sh):
.cache/
# Python bytecode from bin/_*.py helpers:
__pycache__/
*.pyc
# bin/ scripts moved to dotfiles skill SoT — only the README tombstone is committed:
bin/*.sh
bin/*.py
bin/__pycache__/
bin/git-hooks/
!bin/README.md
# Per-machine state — never track across clones. .envrc holds this clone's
# path and may source machine-local credentials:
.envrc
.agents/
.claude/
